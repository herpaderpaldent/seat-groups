language: php

php:
- 7.2.1

services:
  - redis-server

addons:
  hosts:
    -redis

before_script:
- composer self-update
- composer install --no-interaction
- composer dump-autoload

script:
- vendor/bin/phpunit